# Human Subjects Readiness Control Matrix

**Project:** eDelphi  
**Last updated:** 2026-05-02  
**Status key:** Implemented, Partial, Not Started, Blocked, Needs External Review

This matrix maps human-subjects readiness requirements to current implementation, tests, evidence, and launch gaps. It should be updated whenever a readiness-related feature changes.

| ID | Domain | Requirement | Current implementation | Test/evidence target | Current evidence | Status | Launch gap |
|---|---|---|---|---|---|---|---|
| HSR-001 | Consent | Consent must be explicit, versioned, activated before use, and captured with timestamp and participant ID. | Consent routes and store support version creation, activation, and consent records. Participant portal requires acknowledgement before response submission. | Integration test: no active consent blocks response; consent record includes version, timestamp, participant ID. | `server/tests/roadtest.test.mjs`; `server/src/routes/consent.ts`; `server/src/stores/consentStore.ts`; `server/src/routes/participants.ts`. | Implemented | Add export-specific consent evidence and plain-language review. |
| HSR-002 | Withdrawal | Participants must be able to withdraw from future participation, and future submissions must be blocked after withdrawal. | Consent withdrawal routes exist for staff and invitation flow; participant portal exposes withdrawal action for invitees. | Integration test: withdrawal sets timestamp and blocks future response/rating submissions. | `server/tests/roadtest.test.mjs`; `server/src/routes/consent.ts`; `server/src/routes/participants.ts`; `app/src/App.tsx`. | Implemented | Add final participant-facing explanation of withdrawal effects and deletion/retention review workflow. |
| HSR-003 | Confidentiality | Participant materials must truthfully explain that responses are confidential to the research team and linked across rounds by participant IDs; complete anonymity must not be falsely promised. | Study Builder and Participant Portal include confidentiality language; consent generation includes participant ID linkage. | Copy test: no false anonymity promise; participant portal shows confidentiality language before task. | `app/src/core/studyWizard.ts`; `app/src/App.tsx`; app forbidden-language tests. | Partial | Formal plain-language review and configurable non-removable confidentiality clause needed. |
| HSR-004 | Retention | Study records, withdrawal/deletion requests, exports, and retention schedules must be governed, visible, and auditable. | Wizard captures retention schedule; export manifests include retention-related metadata foundations. | Integration test: retention schedule is saved, locked in governance packet, and appears in IRB/export package. | `app/src/core/studyWizard.ts`; `server/src/stores/exportManifestStore.ts`. | Partial | Enforce retention/deletion workflow; add data custodian review and deletion request handling. |
| HSR-005 | Governance signoff | Study launch must require Study Owner and Ethics & Methods Steward signoff before activation/opening. | Study version signoff and activation gates exist; governance screen shows required signoff. | Integration test: activation/opening blocked without required signoffs. | `server/tests/roadtest.test.mjs`; `server/src/studies/routes.ts`; `app/src/App.tsx`. | Implemented | Replace demo-role assumptions with production membership and session model. |
| HSR-006 | Locked consensus rule | Consensus rule must be predefined before Round 1 and locked against mid-study edits. | Study design packet requires consensus rule; backend blocks design changes after governance/round progression. | Test: editing consensus threshold mid-study is blocked; report/export includes threshold and limitation. | `app/tests/*.test.mjs`; `server/tests/roadtest.test.mjs`; `server/src/studies/routes.ts`. | Implemented | Add explicit new-version/correction workflow for exceptional changes. |
| HSR-007 | Round lifecycle | Round transitions must enforce protocol order, open/closed status, and declared terminal round. | Backend round config/state routes enforce prior round closure, published items, declared method boundaries, and terminal rounds. UI next-action flow drives transitions. | Integration test: cannot open later round before prerequisites; Modified Delphi blocks Round 4; Classic Delphi supports Round 4. | `server/tests/roadtest.test.mjs`; `server/src/routes/responses.ts`; `server/src/stores/roundConfigStore.ts`; `app/src/App.tsx`. | Implemented | Add production deadline/reminder scheduler and manual QA evidence for all round variants. |
| HSR-008 | Participant task integrity | Participants must only see currently open tasks; closed rounds must not remain editable; submissions must be reviewable. | Participant portal detects open Round 1 or later structured judgment round; submitted content is shown back with edit/retain path; closed Round 1 setup locks. | End-to-end test: participant can submit, review, revise, retain, then closed round blocks changes. | `app/src/App.tsx`; `server/tests/roadtest.test.mjs`. | Partial | Add browser-level automated tests and mobile/accessibility evidence across Rounds 1-4. |
| HSR-009 | Neutral feedback | Controlled feedback must show group summaries/statistics neutrally and must support retain/revise without pressure. | Feedback panel and participant structured judgment flow use neutral language; later-round choices use verbal labels while preserving coded values. | Copy test: forbidden coercive language absent; participant can retain or revise with equal dignity. | `app/tests/*.test.mjs`; `app/src/App.tsx`; `app/src/policies/governance.ts`. | Partial | Add actual controlled feedback display in participant later-round task with median/IQR/distribution and prior response where appropriate. |
| HSR-010 | AI suggestion labeling | All AI outputs must be labeled "AI Suggestion (Not Final)" and may not be treated as decisions. | AI suggestions store/route and UI cards label outputs; IRB pack draft carries AI suggestion label. | Test: AI output cannot publish without human decision; UI/rendered output includes required label. | `server/tests/roadtest.test.mjs`; `server/src/routes/ai.ts`; `app/src/components/ui/Primitives.tsx`. | Implemented | Add snapshot/export evidence for every AI-assisted package type. |
| HSR-011 | Human AI approval | AI-generated participant-facing content must require explicit Accept/Edit/Reject and configured human signoff before release. | AI suggestion decision and release signoff foundations exist; curation publish gates exist. | Integration test: unaccepted AI suggestion and singly signed AI release are blocked. | `server/tests/roadtest.test.mjs`; `server/src/routes/ai.ts`; `server/src/routes/items.ts`; app governance tests. | Partial | Extend dual release gates to all participant-facing AI-influenced content and feedback configs. |
| HSR-012 | Provenance | Raw anonymized responses, clusters, candidate statements, edits, merge/split rationales, and final wording must be traceable. | Curation desk uses response-derived items, candidate item provenance, merge/split rationale, and audit events. | Integration test: final item traces to source responses and transformation history; rejected/retained minority items remain visible. | `server/tests/roadtest.test.mjs`; `server/src/routes/items.ts`; `server/src/stores/itemStore.ts`; `server/src/stores/mergeActionStore.ts`. | Partial | Build/export graph-friendly provenance bundle and transformation history tables. |
| HSR-013 | Identity/response separation | Identity records and response/rating records must be separated, with no identity-response mapping exposed to unauthorized roles. | Participant master store is separate from response store; response records carry participant IDs; master list access is role-gated and audited. | Test: unauthorized identity access blocked; authorized identity access logs audit event. | `server/tests/roadtest.test.mjs`; `server/src/stores/participantMasterStore.ts`; `server/src/stores/responseStore.ts`; `server/src/routes/participants.ts`. | Implemented | Add production encryption/secrets/deployment controls and formal data-flow documentation. |
| HSR-014 | Authorization | Backend must enforce roles and study membership; UI-only controls are insufficient. | Real user accounts, bearer sessions, study membership assignments, membership removal/downgrade, admin user updates, participant invitation revocation, and role assignment/review UI exist. In session-required mode, route authorization derives role from backend membership rather than client role headers. | Security test: participant cannot access staff views; steward cannot act as PI unless assigned as Owner; revoked invitation cannot submit; role downgrade removes access; manipulated client role header is rejected. | `server/src/middleware/auth.ts`; `server/src/auth/*`; `server/src/studies/store.ts`; `server/src/studies/routes.ts`; `server/src/routes/admin.ts`; `server/src/routes/participants.ts`; `server/tests/roadtest.test.mjs`; `app/src/App.tsx`. | Implemented | Remaining production-session hardening is tracked under HSR-018: cookie/storage strategy, CSRF posture, rate limits, deployment secrets, and security review evidence. |
| HSR-015 | Audit logging | Sensitive actions must be written to append-only, integrity-checkable audit log. | Audit log writes events with sequence/hash chain; database trigger prevents updates; integrity endpoint exists. | Integration test: audit chain verifies; attempted audit mutation fails. | `server/tests/roadtest.test.mjs`; `server/src/core/audit.ts`; `server/src/routes/admin.ts`. | Implemented | Finalize event taxonomy and add incident/correction event categories. |
| HSR-016 | Export logging | Export generation, review, and downloads must be permission-gated and audit logged. | Export package store/routes support manifests, files, review records, downloads, and audit event IDs. | Integration test: export creates package and audit event; review/download creates audit event. | `server/tests/roadtest.test.mjs`; `server/src/routes/reports.ts`; `server/src/stores/exportManifestStore.ts`; `app/src/App.tsx`. | Partial | Complete all package types and redaction controls; add external reviewer evidence. |
| HSR-017 | Accessibility | Participant and staff flows must meet WCAG 2.2 AA expectations. | UI uses semantic controls and responsive layouts; participant portal simplified. | Automated axe scan; keyboard walkthrough; screen reader notes; mobile screenshots. | Current manual visual checks only. | Partial | Complete formal WCAG 2.2 AA audit and fix defects. |
| HSR-018 | Security controls | Application must meet sensitive-data web security baseline: secure sessions, CSRF strategy, validation, encoding, rate limits, headers, dependency review, secrets handling. | Some backend authorization and safe rendering patterns exist; no dangerous HTML rendering in React UI. | OWASP ASVS evidence; dependency scan; access-control abuse tests; XSS/CSRF/rate-limit tests. | `server/tests/roadtest.test.mjs`; `app/tests/*.test.mjs`. | Partial | Production session/CSRF/rate-limit/security-header/secrets/dependency-scanning work remains. |
| HSR-019 | Backup/recovery | Production data must be backed up, restored, and verified without breaking audit integrity. | Database and audit integrity foundations exist. | Restore rehearsal: backup created, restored, migrated, and audit chain verifies. | `server/src/core/database.ts`; `server/src/core/audit.ts`. | Not Started | Define backup schedule, restore tooling, migration rollback, and recovery runbook. |
| HSR-020 | Incident response | Team must be able to pause study, classify incident, document remediation, notify affected parties where applicable, and preserve audit evidence. | Audit history exists; archive preserves records. | Drill: pause study blocks submissions; incident record logs severity, actions, notifications, and resolution. | No dedicated incident workflow yet. | Not Started | Build pause-study state, incident log, notification templates, and response runbook. |
| HSR-021 | IRB/Ethics evidence | IRB/Ethics Pack must clearly present protocol, Delphi suitability, consent/withdrawal/confidentiality, AI disclosure, retention, safeguards, and signoff history. | IRB draft generation foundation exists; export package architecture exists. | Export test: IRB pack includes required sections and imported IRB decision metadata where present. | `server/src/routes/ai.ts`; `server/src/stores/exportManifestStore.ts`. | Partial | Complete IRB pack renderer, import path, approval metadata model, and reviewer-facing evidence. |
| HSR-022 | Final report evidence | Final Delphi Report must include method configuration, response rates, attrition, consensus/near/non-consensus, statistics, limitations, and consensus-not-correctness statement. | Final report/export report exists with round-aware summaries and Office package renderer. | Integration test: final report includes required statement, non-consensus items, stats, and manifest hash. | `server/tests/roadtest.test.mjs`; `server/src/routes/reports.ts`; `server/src/exports/finalReportRenderers.ts`. | Partial | Improve document polish, add full package review, and verify with generated artifacts. |
| HSR-023 | Anonymized dataset | Dataset export must support analysis while excluding identity-response mapping and documenting redaction. | Export package architecture exists; response and rating data stores exist. | Export test: responses/ratings/items/rounds CSV/XLSX contain no identity fields and include data dictionary/redaction manifest. | Foundations in `server/src/routes/reports.ts` and stores. | Partial | Build dedicated anonymized dataset package and redaction manifest workflow. |
| HSR-024 | Audit package | Governance/security reviewers must receive audit events, hashes, access/export events, AI operations, withdrawals, and sensitive actions. | Audit chain and export package store exist. | Export test: audit package includes event chain, integrity hashes, event counts, and category filters. | `server/src/core/audit.ts`; `server/src/stores/exportManifestStore.ts`. | Partial | Build dedicated audit package renderer and verification file. |
| HSR-025 | Release evidence | Human-subjects release must be based on a full dry-run study and documented evidence. | Backend road test covers many workflow paths. | Dry run: create study, signoff, invite, consent, R1, curation, R2/R3/R4, reporting, exports, withdrawal, audit verify, backup restore. | `server/tests/roadtest.test.mjs`. | Partial | Add browser E2E, manual study dry-run transcript, accessibility/security evidence, and operational runbook. |

## Matrix Maintenance Rules

- Do not mark a row **Implemented** unless it has implementation plus current automated or documented manual evidence.
- If a row affects participant rights, identity, AI, exports, or auditability, require Study Owner and Ethics & Methods Steward review before changing status to Implemented.
- If a row depends on deployment environment, mark it Partial until the deployment runbook and evidence exist.
- Every P0 release blocker must map to at least one HSR row.
